{
    "id": "#statementpost",
    "type": [
        {
            "$ref": "#statement"
        },
        {
            "type": "array",
            "items": {
                "$ref": "#statement"
            }
        }
    ]
}